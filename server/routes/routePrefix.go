package routes

// APIPrefix prefixes all API endpoints.
const APIPrefix = "/api"

// DefaultPrefix represents the routing prefix for default
const DefaultPrefix = "/"

// EnvPrefix represents the routing prefix for environment variables
const EnvPrefix = "/env"
